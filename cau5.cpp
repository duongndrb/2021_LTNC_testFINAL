Node* removeMod(Node* head, int m){
    // your code here
    for(Node* p = head; p != NULL; p = p->next)
    {
        if(p->element % m == 0)
        {

        }
    }
    return head;
}
